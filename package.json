{
  "name": "storymatic",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "GPL-3",
  "scripts": {
    "build": "tsc -b; ohm generateBundles --withTypes grammar.ohm && mv grammar.ohm-bundle.js dist/grammar.js && mv grammar.ohm-bundle.d.ts grammar.d.ts"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/zsnout/storymatic"
  },
  "bin": {
    "sm": "./bin/sm"
  },
  "dependencies": {
    "ohm-js": "^16.3.2"
  },
  "devDependencies": {
    "@ohm-js/cli": "^1.1.0",
    "@types/node": "^17.0.29",
    "typescript": "^4.6.3"
  }
}
